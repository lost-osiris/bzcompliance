def __chron_job():
   #Findbugs calls and stuff to update core DB variables
   pass


def find_function(bug_data, params):
   #Sends the passed in bug data to a given script
   #import *params[0]*
   #bug_data = found_function.run(bug_data, params[1:])
   return bug_data
   


if __name__ == "__main__":
   __chron_job()